# Custom Rules for MehSangPromotional

- **Git Push Constraint**: DO NOT run `git push` automatically when completing tasks. Always implement changes, test locally, and make commits, but ONLY push code to the remote GitHub repository when the user explicitly instructs you to push.
